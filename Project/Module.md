모듈(Module) vs 라이브러리(Library)

개발 현장에서 이 두 용어는 혼용되기도 하지만, 기술적인 맥락에서는 다음과 같이 구분한다.

* 모듈 (Gradle/Maven 관점): 프로젝트 내에서 논리적/물리적으로 분리된 빌드
  단위입니다. 각 모듈은 자신만의 build.gradle 파일을 가진다.
* 라이브러리 (결과물 관점): 다른 프로그램에서 가져다 쓸 수 있도록 패키징된
  코드의 집합(보통 .jar 파일)이다.

즉, 내가 만든 특정 모듈이 빌드되어 JAR 파일이 되면, 다른 모듈에게는 라이브러리가 되는 것
